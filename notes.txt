POUR IMPORTER :   ./import.sh --raw --map --city=ch/geneva

POUR LANCER :		cargo run --bin game --release
   si ca marche pas -> cargo run --bin game -- --dev data/system/ch/geneva/maps/center.bin

LES DOCS QUI CONCERNENT LES BUS:
	https://github.com/a-b-street/abstreet/pull/819
	https://github.com/a-b-street/abstreet/issues/372#issuecomment-974331401

Notes sur le talk avec Dustin: 

	abstreet/osm2lane -> pour voir les modifications et indiquer les tags à mettre dans OSM

	modifier une ligne dans osm -> ctrl + s > onglet debug à gauche -> editer dans osm -> comme c'est géofabrik c'est un peu long donc on peut importer la route à la mains
	-> import new city > from json > select the region to import, copy paste the json (geojson.io)
